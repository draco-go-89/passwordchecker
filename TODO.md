# TODO

- [x] Rewrite `style.css` to remove duplicated/unused CSS and match `index.html` structure.
- [ ] Implement coherent theme via CSS variables (colors, blur, shadows, radii).
- [ ] Add particles background styles for `.particles` + `.particle` classes.
- [ ] Style `.navbar`, hero header, `.analyzer-card` (glassmorphism), and buttons.
- [ ] Style strength meter: `.strength-meter`, `.progress`, `#strengthBar`, and `.progress-fill` (ensure JS resizing works).
- [ ] Style result panel (`#result`, `.result-icon`, `.result-text`).
- [ ] Style footer (`.footer`, `.footer-content`, `.social-links a`).
- [ ] Quick manual test: open `index.html` and verify strength bar + layout responsiveness.



